{# NEEDS_UPDATE_CERT_APPROVAL action variant for NEXT_TASK.md #}
{#
Required context:
- drift_details: Vec<DriftDetail>
- update_cert_message: Option<String>
- max_cert_updates: u32
#}
## STOP: Approval Required

Drift detected between current state and baseline certification.

{% if update_cert_message %}
### Update Status

{{ update_cert_message }}

{% endif %}
### Drift Details

{% if drift_details %}
{% for detail in drift_details %}
  - {{ detail.field }}: {{ detail.baseline }} → {{ detail.current }}
{% endfor %}
{% else %}
  (unable to parse drift details)
{% endif %}

### DO NOT PROCEED WITHOUT EXPLICIT APPROVAL

The baseline certification must be updated, but this requires Connor's explicit approval.

### What Changed

Review the drift details above. Common causes:
- Feature file content changed (intentional spec update)
- Step bindings changed (implementation fix)
- Step registry changed (new/modified bindings)

### Instructions

1. Review the drift details carefully
2. **STOP AND WAIT** for Connor's approval
{% if max_cert_updates > 0 %}
3. Autonomous updates are enabled (max_cert_updates={{ max_cert_updates }}). Run tesaki again to auto-update.
{% else %}
3. Autonomous updates are disabled (max_cert_updates=0). Set max_cert_updates in `.tesaki/config.toml` to enable.
{% endif %}
4. Or manually: run `namako update-cert`
