{# Brief for NormalizeIdentityTags mission type #}
{#
Required context:
- feature_path: String
- missing_tags: Vec<String>
#}
**Title:** Normalize identity tags in {{ feature_path }}

**Objective:** Ensure required identity tags are present and correct.

**Context:**
Missing tags: {% if missing_tags %}{{ missing_tags | join(", ") }}{% else %}unknown{% endif %}

**Validation Criteria:**
1. Lint passes with identity tags fixed
