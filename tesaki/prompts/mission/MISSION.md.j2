{# Main mission prompt template #}
{#
Required context:
- mission_id: String
- mission_type: String
- stage: String
- target: Option<String>
- objective: String
- context: String
- validation_criteria: Vec<String>
- surface_policy: {spec, tests_bindings, sut}
- surface_definitions: {spec: {patterns}, tests_bindings: {patterns}, sut: {patterns}}
- budgets: {max_files_changed, max_scenarios_promoted, max_runtime_seconds}
- version: String
- previous_mission: Option<{type, outcome, notes}> (optional)
- expected_cascade: Option<String> (optional)
#}
{% include "components/critical_constraints.md.j2" %}

# Mission {{ mission_id }}

**Type:** {{ mission_type }}
**Stage:** {{ stage }}
{% if target %}**Target:** {{ target }}{% endif %}

---

> **Budget:** max {{ budgets.max_files_changed }} file(s) changed, max {{ budgets.max_scenarios_promoted }} scenario(s) promoted

## 🎯 Objective

{{ objective }}

---

## Surface Policy Summary

| Surface | Policy |
|---------|--------|
| Spec | {{ surface_policy.spec | upper }} |
| Tests/Bindings | {{ surface_policy.tests_bindings | upper }} |
| SUT | {{ surface_policy.sut | upper }} |

*(See constraints at top for allowed file patterns)*

---

## Why this mission

{{ context }}

{% if selection_evidence %}
**Selection Evidence:**
- Type: {{ selection_evidence.issue_type }}
{% if selection_evidence.file_path %}- File: `{{ selection_evidence.file_path }}`{% endif %}
{% if selection_evidence.line %}- Line: {{ selection_evidence.line }}{% endif %}
- {{ selection_evidence.description }}
{% if selection_evidence.example %}- Example: {{ selection_evidence.example }}{% endif %}
{% endif %}

{% if previous_failure %}
---

## ⚠️ Previous Mission Failed - Learn From This

**Previous Mission:** {{ previous_failure.mission_type }}
{% if previous_failure.target %}**Target:** {{ previous_failure.target }}{% endif %}
**Failure Reason:** {{ previous_failure.stop_reason }}

{% if previous_failure.violated_files %}
### 🚫 These Files Were LOCKED and Caused Failure:

{% for file in previous_failure.violated_files %}
- `{{ file }}`
{% endfor %}

**DO NOT modify these files again.** They are in a **{{ previous_failure.violated_surface }}** surface that is LOCKED.

{% if previous_failure.details %}**Additional Context:** {{ previous_failure.details }}{% endif %}

**What to do instead:**
1. Work only within the ALLOWED surfaces (see constraints at top)
2. If this fix genuinely requires editing locked files, STOP and report that constraint
3. Consider alternative approaches that don't require locked files
{% else %}
{% if previous_failure.details %}- **Details:** {{ previous_failure.details }}{% endif %}

**Learn from this failure and try a different approach.**
{% endif %}
{% endif %}

{% if previous_mission %}
---

## Previous Mission Context

**Previous:** {{ previous_mission.type }}
**Outcome:** {{ previous_mission.outcome }}
{% if previous_mission.notes %}
**Notes:** {{ previous_mission.notes }}
{% endif %}
{% endif %}

{% if expected_cascade %}
---

## Expected Cascade

{{ expected_cascade }}

This is normal in spec-driven development. The next mission will address the downstream work.
{% endif %}

---

## ✅ Validation Criteria

{% for criterion in validation_criteria %}
{{ loop.index }}. {{ criterion }}
{% else %}
1. `namako gate --json` must pass
{% endfor %}

**Do NOT run `namako` commands or any build/test commands (`cargo`, etc.). Tesaki will validate.**

---

## Budgets

| Limit | Value |
|-------|-------|
| Max files changed | {{ budgets.max_files_changed }} |
| Max scenarios promoted | {{ budgets.max_scenarios_promoted }} |
| Max runtime (seconds) | {{ budgets.max_runtime_seconds }} |

---

*Tesaki v{{ version }}*
