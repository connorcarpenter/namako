# rust-analyzer configuration for namako workspace

[files]
exclude = ["target", "_WORKSPACE/target"]

[procMacro]
enable = false

[check]
command = "clippy"

[cargo]
allTargets = false

[completion]
limit = 100

[workspace.symbol.search]
limit = 100
