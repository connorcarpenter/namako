#!/usr/bin/env bash

set -euo pipefail

SCRIPT_PATH="$(readlink -f "${BASH_SOURCE[0]}")"
REPO_ROOT="$(cd "$(dirname "$SCRIPT_PATH")/.." && pwd)"

exec cargo run --manifest-path "$REPO_ROOT/Cargo.toml" -p tesaki -- "$@"
